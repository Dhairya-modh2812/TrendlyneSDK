import React from 'react';
import { Search } from '../components/Search'

function SeachedData(props) {
    const { onPageSearch } = props;

  return <div>
 {/* <Search
              onPageSearch={(data) => {
                setindIvidualPosts(data);
              }}
              
            />             */}
            
              </div>;
}

export default SeachedData;
